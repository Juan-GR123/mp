#include <iostream>
#include "ACE.h"
#include <cstring>
using namespace std;

// pedir por consola un numero decimal que debes pasar a binario cojer los restos los pones en el array reglas y apartir del numero que te den imprimes la regla
// de la grafica correspondiente
int main()
{
    int regla;
    int TMAX;

    bool reglas[8];
    bool ACE[100];

    cout << "Dime un numero menor que 256" << endl;
    cin >> regla;

    cout << "Ahora indique el numero de iteraciones a realizar" << endl;
    cin >> TMAX;
}