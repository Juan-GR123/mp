#ifndef ACE_H // esto puede dar errores por tener el nombre en mayuscula ?
#define ACE_H

#include <string>
using namespace std;

const int MAX_SIZE = 100;

/**
 * @brief devuelve los colores que devolvera
 * @param state valores de entrada en formato boolean
 */
void mostrar(const bool state[], int tam);

#endif;
